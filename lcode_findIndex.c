#include <stdio.h>
#include <stdlib.h>
typedef int (*testFunc)(int);

int findIndex(int *arr, int size, int (*testFunc)(int))
{
    return (1);
}

int isPrime(int num)
{

    return (1);
}

int main( void )
{
    int arr[] = {10, 2, 3, 44, 1, 5};
    int size = sizeof(arr) / sizeof(arr[0]);
    printf("%zu\n", sizeof(arr));
}